{
  "name": "lagao",
  "private": true,
  "version": "1.0.0",
  "description": "Lagao storefront with full-stack admin panel",
  "scripts": {
    "dev": "npm-run-all --parallel dev:api dev:web dev:store",
    "dev:api": "npm --prefix admin-api run dev",
    "dev:web": "npm --prefix admin-web run dev",
    "dev:store": "npm --prefix customer-web run dev",
    "build": "npm --prefix admin-api run build && npm --prefix admin-web run build && npm --prefix customer-web run build",
    "lint": "npm --prefix admin-web run lint && npm --prefix customer-web run lint"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
